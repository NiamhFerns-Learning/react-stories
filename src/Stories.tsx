import { Story, StoryList } from './Story';

export function Stories({ stories, storyLists }: { stories: Story[], storyLists: StoryList[] }) {
  // Temp var for sketching.

  return (
    <>
      <p>Hi</p>
    </>
  );
}
